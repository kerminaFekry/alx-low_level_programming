#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0
 */
int main(void)
{
int number;

for (number = 48; number <= 57; number++)
{
putchar(number);
}
putchar(10);
return (0);
}
