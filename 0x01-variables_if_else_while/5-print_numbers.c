#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0
 */
int main(void)
{
int number;

for (number = 0; number < 10; number++)
{
putchar(int number);
}
putchar(10);
return (0);
}
